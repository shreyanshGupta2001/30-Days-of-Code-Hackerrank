//Language C

int main()
{
    int N = parse_int(ltrim(rtrim(readline())));
    
    scanf("%d",&N);
    
    if( N%2 == 1 || (N > 5 && N < 21))
    
        printf("Weird");
        
        else
        
            printf("Not Weird");

    return 0;
}
